adil1248.github.io — personal professional site.

Source of truth lives in the OpenClaw workspace at sites/personal/ and is mirrored here for GitHub Pages.
No JavaScript, no trackers, no third-party assets.

Deploy (from the workspace):

    git --git-dir=/var/home/adil/.cache/adil1248-pages.git --work-tree=. add index.html README.md .nojekyll
    git --git-dir=/var/home/adil/.cache/adil1248-pages.git --work-tree=. commit -m "..."
    git --git-dir=/var/home/adil/.cache/adil1248-pages.git --work-tree=. push origin main

preview.png is a local render check and is intentionally not published.
